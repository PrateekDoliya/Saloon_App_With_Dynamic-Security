server:
  port: 8083

spring:
  application:
    name: SERVICES-PROVIDED
  datasource:
    url: jdbc:mysql://localhost:3306/maikhane_provided_service_saloon_app
    username: root
    password: tiger
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
jwt:
  secret: springSecurity
  jwtExpirationInMs: 600000
  
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true